<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Maquillage Chic{% endblock %}</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">

    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <style>
        /* Palette féminine et chic */
        body {
            font-family: 'Segoe UI', sans-serif;
            background-color: #fff6f9;
            color: #333;
        }

        .custom-navbar {
            background-color: #f8d4e4; /* Rose poudré */
            padding: 15px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
        }

        .navbar-brand {
            font-family: 'Georgia', serif;
            font-size: 2rem;
            color: #b03060 !important; /* Framboise */
        }

        .navbar-nav .nav-link {
            color: #5e2750 !important;
            font-size: 1.1rem;
            margin-right: 20px;
        }

        .navbar-nav .nav-link:hover {
            color: #e91e63 !important; /* Rose foncé */
        }

        .btn-logout {
            background: transparent;
            border: none;
            cursor: pointer;
        }

        .btn-logout svg {
            width: 28px;
            height: 28px;
            fill: #5e2750;
        }

        .btn-logout:hover {
            transform: scale(1.1);
        }

        .custom-footer {
            background-color: #f3e5f5;
            color: #5e2750;
            padding: 30px 0;
            font-size: 1rem;
            border-top: 1px solid #e1bee7;
        }

        .custom-footer p {
            margin: 0;
            text-align: center;
        }

        main.container {
            padding-top: 30px;
        }
    </style>
</head>
<body>

    {% include 'pages/header.html.twig' %}

    <main class="container mt-4">
        {% block body %}{% endblock %}
    </main>

    {% include 'pages/footer.html.twig' %}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
